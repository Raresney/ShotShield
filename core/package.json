{
  "name": "@shotshield/core",
  "version": "0.0.0",
  "description": "Detects sensitive data in text. No runtime dependencies.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "test": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
